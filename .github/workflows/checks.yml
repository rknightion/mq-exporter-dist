name: Checks
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  checks:
    name: ci-success
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4
        with:
          just-version: '1.58.0'
      - id: tools
        run: just ci-tool-versions >> "$GITHUB_OUTPUT"
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ steps.tools.outputs.go }}
          cache: false
      - run: just check
      - run: just public-check
  actionlint:
    uses: rknightion/.github/.github/workflows/actionlint.yml@8d14adaa295c4a7670b1d4e744b070955b710598 # v1.24.0
    permissions:
      contents: read
  zizmor:
    uses: rknightion/.github/.github/workflows/zizmor.yml@8d14adaa295c4a7670b1d4e744b070955b710598 # v1.24.0
    permissions:
      contents: read
      actions: read
      security-events: write
